/**
 * Options sample example
 */
module.exports = exports = {
  /**
   * This string should exactly match the filename of the library (without the
   * file extension).
   */
  type: 'example',

  /**
   * any other options needed by the runner should be defined and described here
   */
};
